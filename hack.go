package main

import (
	"fmt"
	//"hash"
	//"list"
	"math"
	/*
	"math"
	"runtime"
	"time"
	"sort"
	*/
)

func main() {
	fmt.Println("Hello, CS98! Its so nice to meet you! I am Sasha's super cool program made while learning Golang! This message was printed to you using the fmt directory which allows you to take inputs and produce outputs!")
	
	var myCoolArray [25]int 
	for i := 0; i < 25; i++ {
		myCoolArray[i] = i
	}

/*
	//create new hashtable
	const arrayLength = 50
	type hashtable struct {

		data[arrayLength]*linkedlist.LinkedList
	}
*/
	
}